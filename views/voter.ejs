<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Voter Side</title>
    </head>
    <body>
        <main>
            <div style="padding: 10px;">
                <a href="<%=baseURL%>/">Back to home page</a>   
                
            </div>
        
            <% if (msgCode == "VOTED") { %>
            <div style="color:green">Voted Successfully ❤</div>
            <% } else { %>
                <div style="margin: 5px";>Student ID</div>

                <form action="voter/castVote" method="post">
                    <input type="text" id="studentID" name="studentID" style="margin: 5px";>
                    <table>
                        <% for(var i = 0; i < candidates.length; i++) { %>
                        <tr>
                            <td>
                                <input
                                    type="radio"
                                    name="voteRadio"
                                    value="<%= candidates[i].candidateID %>"
                                />
                            </td>
                            </td>
                            <td><%= candidates[i].candidateID %></td>
                            <td><%= candidates[i].candidateName %></td>
                        </tr>
                        <%}%>
                    </table>    
                    <input type="submit" value="Cast Vote" style="margin: 5px;">
    
                </form>
                <% } %>
        </main>
    </body>
</html>
